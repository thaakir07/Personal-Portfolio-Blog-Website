/**
 * Returns a styled div element with a background image.
 *
 * The background element is styled with CSS from the Background.css file.
 * @returns A styled background component.
 */
function Background() {
  return (
    <div className="background"></div>
  );
}

export default Background;